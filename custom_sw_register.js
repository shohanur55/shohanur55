// Cache-first notes only.
// Flutter generates flutter_service_worker.js at build time, so the real caching
// strategy should be handled by the generated worker and the build output.
// Keep this file as a reference for future manual service worker experiments.